apiVersion: v1
kind: Pod
metadata:
  name: sample-resource-only-limits
spec:
  containers:
  - name: nginx-container
    image: nginx:latest
    resources:
      limits:
        memory: 256Mi
        cpu: 200m